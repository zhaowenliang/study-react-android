rootProject.name = 'StudyReactNative'

// 使用cli-platform-android自定创建"PackageList"，每次构建项目在文件内部，自动处理PackageManager。
// 在"ReactNativeHost"的实现方法"getPackages()"中，需要加入PackageList getPackages()。
apply from: file("../node_modules/@react-native-community/cli-platform-android/native_modules.gradle"); applyNativeModulesSettingsGradle(settings)

include ':app'
